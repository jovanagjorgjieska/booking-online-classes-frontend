const Navbar = () => {
    return ( 
        <nav className="navbar">
            <h1>SwiftLearn</h1>
            <a href="/">Home</a>
            <a href="/profile">Profile</a>
            <a href="/logout">Log out</a>
        </nav>
    );
}

export default Navbar;
